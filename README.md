# raspian-jessie-python3
Install script for python3 on a Jessie based Raspian distribution

## Usage

Using `pyenv`:
* `bash install.sh`

Using manual build from sources:
* `INSTALL_PYTHON=manual bash install.sh`
